# 고객용 키오스크 프론트 최신 추가분 테스트 체크리스트

- 프로젝트: 분식집 키오스크 고객용 웹 (kiosk-customer)
- 대상 기능: 옵션카드 3개 이상 슬라이드 (`DragScrollRow.jsx`, `SetMenuModal.jsx`)
- 관련 커밋: `bc28791b` 옵션카드 3개 이상시 슬라이드 가능
- 작성일: 2026-08-10
- 테스트 담당자: 박유진
- 테스트 기기: **\*\*\*\***\_\_\_\_**\*\*\*\***
- OS / 브라우저: **\*\*\*\***\_\_\_\_**\*\*\*\***

## 결과 기준

- 통과: 예상 결과와 동일하게 동작
- 실패: 오류가 발생하거나 예상 결과와 다르게 동작
- 미실시: 아직 확인하지 않음

## 사전 조건

- kiosk-customer 프론트가 로컬에서 실행 중이어야 한다 (`npm start`).
- 백엔드 서버가 실행 중이어야 한다.
- 옵션 3개 이상인 세트메뉴와 3개 미만인 세트메뉴가 각각 하나 이상 존재해야 한다.

## 기능 테스트

| ID    | 구분                | 테스트 방법                                          | 예상 결과                                               | 결과 | 실제 결과 및 비고 |
| ----- | ------------------- | ---------------------------------------------------- | ------------------------------------------------------- | ---- | ----------------- |
| CS-01 | 기본 표시           | 옵션 카드 3개 미만인 세트메뉴 모달 열기              | 카드가 한 줄에 모두 표시되고 슬라이드 동작이 없다.      | 통과 |                   |
| CS-02 | 슬라이드            | 옵션 카드 3개 이상인 세트메뉴 모달 열기              | 카드 목록이 가로로 넘치고 마우스 드래그로 슬라이드된다. | 통과 |                   |
| CS-03 | 드래그 후 클릭 차단 | 카드 영역을 드래그(5px 이상 이동)한 뒤 마우스를 뗀다 | 드래그로 인식되어 카드가 선택(클릭)되지 않는다.         | 통과 |                   |
| CS-04 | 일반 클릭           | 드래그 없이 카드를 바로 클릭                         | 옵션 카드가 정상적으로 선택된다.                        | 통과 |                   |
| CS-05 | 영역 이탈           | 드래그 중 마우스 포인터가 카드 영역 밖으로 나감      | 드래그가 멈추고 이후 정상 상태로 복귀한다.              | 통과 |                   |
| CS-06 | 터치 조작           | 실기기(터치스크린)에서 옵션 카드 좌우로 슬라이드     | 손가락 터치로 좌우 슬라이드가 정상 동작한다.            | 통과 |                   |
| CS-07 | 카드 수량           | 옵션 카드가 5개, 10개 이상인 경우 각각 확인          | 카드 개수와 무관하게 슬라이드 범위가 끝까지 도달한다.   | 통과 |                   |
| CS-08 | 모달 재오픈         | 모달을 닫았다가 동일 메뉴로 다시 열기                | 슬라이드 위치가 초기 상태로 리셋되어 표시된다.          | 통과 |                   |
| CS-09 | 빌드 정상화         | build 산출물 제거 후 `npm run build` 재실행          | 오류 없이 빌드가 완료된다.                              | 통과 |                   |

## 자동 테스트 실행 기록

| ID      | 테스트 대상                    | 실행 결과                          | 결과 | 비고                                                                                                                 |
| ------- | ------------------------------ | ---------------------------------- | ---- | -------------------------------------------------------------------------------------------------------------------- |
| AUTO-01 | 고객용 프론트 전체 자동 테스트 | 11개 테스트 묶음, 49개 테스트 실행 | 통과 | `jest --clearCache` 후 재실행하여 전부 통과. Menu.test.jsx의 console.error는 네트워크 오류 처리 테스트의 의도된 로그 |

## 실패 항목 기록

| 발생 ID | 발생 시각  | 증상                                           | 재현 방법                           | 콘솔·네트워크 오류                                                                                                            | 조치 결과                                                  |
| ------- | ---------- | ---------------------------------------------- | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| FAIL-01 | 2026-08-10 | `MenuCard.test.jsx` 스위트가 실행 자체가 안 됨 | `npm test -- --watchAll=false` 실행 | `jest: failed to cache transform results ... EPERM: operation not permitted, rename ...jest-transform-cache...setupTests_...` | 해결. `npx jest --clearCache` 후 재실행하여 정상 통과 확인 |

## 최종 결과

- 자동 테스트: 통과 (49/49, 11개 스위트 전부 통과)
- 기능 테스트: 통과 9건 / 실패 0건 / 미실시 0건
- 개선 필요: 없음
- 최종 판정: 통과
